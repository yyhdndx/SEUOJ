# Optional environment overrides for seu-oj-backend.
# Copy to .env and load before starting the server, or set variables in your shell.

SERVER_HOST=0.0.0.0
SERVER_PORT=8080

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=seu_oj

REDIS_ADDR=127.0.0.1:6379
REDIS_PASSWORD=
REDIS_DB=0

JWT_SECRET=change-me-before-demo

SANDBOX_DOCKER_IMAGE=gcc:13
